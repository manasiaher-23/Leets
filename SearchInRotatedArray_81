class Solution {
    public boolean search(int[] nums, int target) {
    boolean ans=false;
    for(int i=0;i<nums.length;i++){
        if(nums[i]==target){
            ans= true;
        }
    }
    if(!ans){
        return false;
    }else{
        return true;
    }}
    
    public static void main(String[] args){
        Scanner o=new Scanner(System.in);
        int n=o.nextInt();
        int[]nums=new int[n];

        for(int i=0;i<n;i++){
            nums[i]=o.nextInt();
        }
        int target =o.nextInt();
        Solution s=new Solution();
        boolean found=s.search(nums, target);
    }
}
